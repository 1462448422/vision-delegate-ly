# vision-delegate-ly 独立插件

`@deepseek-ai/dsh-vision-delegate` 的独立可移植版：当所选模型不支持图片输入时，由图像模型（自动发现或显式配置）在消息进入步骤前描述用户附带的图片，纯文本模型路由因此也能"看图"。

## 文件说明

- `src/` — TypeScript 源码（改动后需重新构建）
- `lib/` — 编译产物（`lib/index.js` 是运行时入口，**部署直接用它，无需构建**）
- `tests/` — 单元测试（需要 deepseek-harness monorepo 环境才能运行，仅供参考）
- `package.json` — 独立依赖声明（peer 全部 optional，运行时由 DSH 安装提供）

## 在另一台 DSH 电脑上安装

### 方法一：从 GitHub 直接安装（推荐）

在对方 profile 的 `package.json` `dependencies` 里加：

```json
"@deepseek-ai/dsh-vision-delegate": "github:1462448422/vision-delegate-ly#main"
```

然后在 profile 目录执行 `pnpm install`。

### 方法二：本地 file: 依赖

把本文件夹拷到对方机器（如 `D:/plugins/dsh-vision-delegate`），profile `package.json` 加：

```json
"@deepseek-ai/dsh-vision-delegate": "file:D:/plugins/dsh-vision-delegate"
```

然后 `pnpm install`。

### 加配置并重启

对方 profile 的 `cordis.patch.yml` 加：

```yaml
- insert:
    - id: vision-delegate-ly
      name: '@deepseek-ai/dsh-vision-delegate'
```

重启 DSH，插件列表出现 `vision-delegate-ly` 即成功。再确认注册了能看图的模型（如 `llm-pi-ai` 下的 qwen3.7-plus），否则插件保持惰性。

## ⚠️ 版本对齐

`package.json` 里的 peer 版本（`@deepseek-ai/cordis` `4.0.1`、`dsh-llm` `0.1.0-rc.5` 等）必须与对方 DSH 的内部包版本一致，否则 Service 接口对不上会加载报错。对方可在 `C:\...\.dsh\profiles\node_modules\@deepseek-ai\dsh-llm\package.json` 查看实际版本，不一致就改本文件的 peer 版本号。

## 重新构建（改了 src/ 之后）

需要 node + pnpm + 本仓库 monorepo 环境，或手工在 monorepo 中跑 `pnpm build` 后把新的 `lib/` 拷回本目录。